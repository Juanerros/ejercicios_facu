#include <stdio.h>
int main() {
    //calcular el factorial de un numero dado usando for multiplica susecivamente los numeros desde 1 hasta el numero
  int ;
  //son numeros reales enteros ej: 6,7,8
  // float ;
  //son numreos reales ej: 8.7 ,8,6
  // char :
  //son caracteres ej: a
  // stirng;
  // son unas continuaicons de caracteres ej: hola (generalmente palabras) 
  //  caracteres espeiales de todaos if,while,while do,for
  // "//=o las dos barras es igial a o"
  // "? es igual a negado "
  // && es igual a y
  // if se usa que cuando se cumpla una condicion realice cierta accion
  // ejemplo: if (i==0)[]
  for (int i = 0; i < 4; i++) {
    printf("Ingresa edad: \n");
    scanf("%d", &edades[i]);
    acum += edades[i];
  }

  
  return 0;
}