#include <stdio.h>

int main() {
    int num[];
    int posi, min minPosi, pares;


    do{
        printf("Ingresa un numero:\n");
        scanf("%d", &num);
        if(num)
    } while(num != 0);


    return 0;
}