#include <stdio.h>

int main() {
    int num1, num2;
    char ope;
    float result = 0;

    printf("Ingresa el primer numero:\n");
    scanf("%d", &num1);

    printf("Ingresa el segundo numero:\n");
    scanf("%d", &num2);

    printf("Ingresa la operacion suma(S)|resta(R)|producto(P)|cociente(C):\n");
    scanf("%d", &ope);

    switch(ope) {
        case "S":
            printf("Resultado de suma: "+ );
            break;
        case "S":
            printf("Resultado de suma: "+ );
            break;
        case "S":
            printf("Resultado de suma: "+ );
            break;
        case "S":
            printf("Resultado de suma: "+ );
            break;
            
        
        default: 
            printf("ERROR: entrda invalida \n");
            break;
    }

    return 0;
}